# Setup

## Requirements

Before starting, make sure you have installed

- Git
- Docker
- Docker Compose
- Python 3.11+
- PostgreSQL (optional)

## Verify the installation

```bash
git --version
docker --version
python --version
```
